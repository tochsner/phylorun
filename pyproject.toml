[project]
name = "phylorun"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "click>=8.3.0",
    "pyright>=1.1.406",
    "ruff>=0.14.0",
]

[project.scripts]
phylorun = "phylorun.main:cli"
